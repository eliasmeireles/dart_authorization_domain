name: dart_authorization_domain
description: Websocket domain

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  json_annotation: ^4.4.0
  dart_net_work:
    git:
      url: https://github.com/eliasmeireles/dart_net_work.git

  get_it: ^7.2.0
  injectable: ^2.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
  build_runner:
  json_serializable: ^6.1.3
  retrofit_generator: ^7.0.1+1
  injectable_generator: